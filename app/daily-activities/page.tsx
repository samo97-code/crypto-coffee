import Wrapper from "@/components/activities/Wrapper";

export const metadata = {
    title: 'Daily Activities',
};

const DailyActivitiesPage = () => {
    return (
        <Wrapper/>
    )
}

export default DailyActivitiesPage
